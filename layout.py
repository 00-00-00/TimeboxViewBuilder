from canvas import Canvas

class Layout:
    def __init__(self, canvas):
        self.__canvas = canvas
        self.__matrix = []
    
    def addRow(views):
        for view in views:
            view.get_width() 
            
